class incorrectNumberOfComponents extends RuntimeException {
    public incorrectNumberOfComponents(String errorMessage) {
        super(errorMessage);
    }
}

class incorrectPhoneNumberFormat extends RuntimeException {
    public incorrectPhoneNumberFormat(String errorMessage) {
        super(errorMessage);
    }
}

class incorrectEmailFormat extends RuntimeException {
    public incorrectEmailFormat(String errorMessage) {
        super(errorMessage);
    }
}
class personIsNotFound extends RuntimeException {
    public personIsNotFound(String errorMessage) {
        super(errorMessage);
    }
}

